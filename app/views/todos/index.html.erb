<h1>Listing all Todo's</h1>
<table>
  <tr>
    <th>Name</th>
    <th>description</th>
  </tr>
  <% @todos.each do |todo| %>
    <tr>
      <td><%= todo.name %></td>
      <td><%= todo.description %></td>
      <td><%= link_to "Edit", edit_todo_path(todo)%></td>
      <td><%= link_to "Show", todo_path(todo)%></td>
    </tr>
  <% end %>
</table>
<p><%= link_to "Create Todo", new_todo_path %></p>
